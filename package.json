{
  "name": "nearify-web",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "generate:sitemap": "node scripts/generate-sitemap.js > sitemap.xml"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.106.1",
    "vitest": "^1.6.0"
  }
}
